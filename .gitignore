# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/
# Package.resolved is committed for reproducible dependency pins.

# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
*.xcuserstate

# Secrets & environment — never commit
.env
.env.*
!.env.example
.envrc
*.pem
*.p12
credentials.json

# Local project memory / agent config (machine-local, not part of the repo)
CLAUDE.md
.claude/

# Local agent & editor tooling
.codegraph/
.cursor/
.gemini/
.tldr/
.tldrignore
.mcp.json
opencode.jsonc

# Local clinical sample data — PHI must never be committed
samples/private/
*.bridge.json
# Raw-response eval log (#4): local + nukable; default lives under dataRoot (outside the repo),
# but defend against an in-repo dataRoot.
raw-responses.jsonl
raw-responses/

# bridge-eval harness — synthetic fixtures are committed in Tests/; these are the
# local-only Tier B real-PHI fixtures and run artifacts. Never commit.
eval/fixtures/
eval/runs/
